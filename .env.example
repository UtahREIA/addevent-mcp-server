# API token from your AddEvent dashboard: Settings > API
# https://dashboard.addevent.com -> Settings -> API
ADDEVENT_API_KEY=

# Only needed when running the HTTP transport locally (npm run dev / npm start with TRANSPORT=http).
# Vercel sets PORT automatically in production.
PORT=3000
